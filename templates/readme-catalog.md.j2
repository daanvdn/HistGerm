<!-- histgerm-catalog:start -->
## Captured resources

<!-- Generated by scripts/generate_readme.py; do not edit this section directly. -->

### Corpora ({{ corpora | length }})

| Resource | ID | Language stages | Availability | Reviewed | Description |
| --- | --- | --- | --- | --- | --- |
{% for row in corpora -%}
| {% if row.url %}[{{ row.name | markdown_cell }}]({{ row.url }}){% else %}{{ row.name | markdown_cell }}{% endif %} | `{{ row.id }}` | {{ row.stages | join(", ") | markdown_cell }} | {{ row.availability | join(", ") | markdown_cell }} | {{ row.reviewed_on }} | {{ row.description | markdown_cell }} |
{% endfor %}

### Dictionaries ({{ dictionaries | length }})

| Resource | ID | Language stages | Availability | Features | Reviewed | Description |
| --- | --- | --- | --- | --- | --- | --- |
{% for row in dictionaries -%}
| {% if row.url %}[{{ row.name | markdown_cell }}]({{ row.url }}){% else %}{{ row.name | markdown_cell }}{% endif %} | `{{ row.id }}` | {{ row.stages | join(", ") | markdown_cell }} | {{ row.availability | join(", ") | markdown_cell }} | {{ row.details | join(", ") | markdown_cell }} | {{ row.reviewed_on }} | {{ row.description | markdown_cell }} |
{% endfor %}

### Tools ({{ tools | length }})

| Resource | ID | Language stages | Availability | Tasks | Reviewed | Description |
| --- | --- | --- | --- | --- | --- | --- |
{% for row in tools -%}
| {% if row.url %}[{{ row.name | markdown_cell }}]({{ row.url }}){% else %}{{ row.name | markdown_cell }}{% endif %} | `{{ row.id }}` | {{ row.stages | join(", ") | markdown_cell }} | {{ row.availability | join(", ") | markdown_cell }} | {{ row.details | join(", ") | markdown_cell }} | {{ row.reviewed_on }} | {{ row.description | markdown_cell }} |
{% endfor %}
<!-- histgerm-catalog:end -->
